{
  "name": "data-extractor-server",
  "version": "1.0.0",
  "description": "DSF Data Extractor web app that lets the server query the script instead of the client",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Lotsos",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^1.19.0"
  }
}
